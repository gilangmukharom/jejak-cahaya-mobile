<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ══ Izin Jejak Cahaya ═══════════════════════════════════════════════
         Lokasi dan kamera adalah inti permainan, bukan pelengkap: tanpa lokasi
         geofence masjid (Layer 1) dan proximity checkpoint (Layer 2) tidak bisa
         dievaluasi, dan tanpa kamera QR checkpoint (Layer 3) tidak bisa dipindai.
    -->

    <!-- Lokasi presisi diperlukan karena radius checkpoint hanya ±25 m;
         ACCESS_COARSE_LOCATION saja meleset terlalu jauh untuk itu. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Pemindaian QR di checkpoint. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ══ Pengingat waktu sholat ══════════════════════════════════════════
         Pengingatnya dititipkan ke alarm sistem, bukan ditunggu oleh aplikasi
         yang berjalan di latar belakang. Itu berarti sistem yang perlu diberi
         tahu dua hal: bahwa aplikasi boleh menampilkan notifikasi, dan bahwa
         alarmnya perlu berbunyi tepat menit.

         SCHEDULE_EXACT_ALARM dipilih alih-alih USE_EXACT_ALARM: yang kedua
         diberikan tanpa bertanya, tetapi hanya boleh dipakai aplikasi jam
         alarm dan kalender, dan Play Store mengauditnya. Yang pertama meminta
         persetujuan pengguna lebih dulu — dan bila ditolak, jadwalnya tetap
         terpasang, hanya dengan ketelitian beberapa menit.

         RECEIVE_BOOT_COMPLETED diperlukan karena seluruh alarm dihapus sistem
         setiap kali perangkat dimatikan; tanpa izin ini pengingat berhenti
         diam-diam sesudah restart pertama. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- GPS wajib ada di perangkat; tanpa itu aplikasi tidak dapat berfungsi
         sama sekali, jadi lebih baik tidak muncul di Play Store untuk perangkat
         tersebut daripada terpasang lalu gagal dipakai. -->
    <uses-feature android:name="android.hardware.location.gps" android:required="true" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <!-- Kompas kiblat. Tidak diwajibkan: perangkat tanpa magnetometer tetap
         bisa memakai seluruh aplikasi, dan layar kiblat menampilkan sudutnya
         sebagai angka ketika jarumnya tidak bisa digerakkan. -->
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />

    <application
        android:label="Jejak Cahaya"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Penerima milik flutter_local_notifications. Yang pertama
             menampilkan notifikasi ketika alarmnya berbunyi; yang kedua
             memasang ulang seluruh jadwal sesudah perangkat dinyalakan atau
             aplikasinya diperbarui — dua peristiwa yang menghapus alarm. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
